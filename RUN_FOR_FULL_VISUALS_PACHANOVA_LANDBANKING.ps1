# PACHANOVA LANDBANKING - PERSISTENT VISUALS CMD (post autonomous F1-6 + F7 evolution + Official Google Antigravity SDK alignment)
# See docs/antigravity-official-alignment.md and updated ~/.grok/skills/antigravity/SKILL.md
# (https://github.com/google-antigravity/antigravity-sdk-python - Conversation state, Triggers, scoped Subagents, MCP, Hooks/Policies)
# Run this in YOUR OWN terminal (pwsh) for unbroken view of ALL advances.
# Hard refresh (Ctrl+Shift+R) after start to see holograms, E2E, 5PNC real orq, Master, full project.

cd 'C:/Users/LENOVO/Desktop/labotarorio lihue/pachanova'

# Optional demo db (if Docker Desktop running):
# pnpm run demo:db:up
# pnpm run demo:db:migrate
# pnpm run demo:db:seed

# Main: start the unified dashboard (single project 3000)
pnpm --filter dashboard run dev

# Then open:
# http://localhost:3000/dashboard/admin/landbank   <-- Master + E2E sequential buttons + holograms + 5PNC real (PAR 68112.5 net @31639 eff 17.1% power 3250 Fases/Master)
# http://localhost:3000/dashboard/investor         <-- Hub + portfolio + P2P + credits/borrow + yields + gov + holograms + 'ver avances'
# http://localhost:3000/dashboard/investor/yields
# http://localhost:3000/dashboard/investor/marketplace (or /marketplace)

# Rich demo permanent: always shows DATOS REALES even no DB.
# Landbanking = full PachaNova + tools. Master sacred. Autonomy active (blackboard + subagents).
# Sub post-F6 running for more (orq vis, bootstrap script, etc).
# Never stop.
