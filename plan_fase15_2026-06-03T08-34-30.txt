# Plan Fase 15 (Orchestrator Agent): Full E2E Verifiable Onchain Proofs + Interactive Certs + Live PNC Net Portfolio Cards + Borrow Lock Accrual + Gov Mail Alerts + Consistent Recompute (Fase35-38/41 completion for dedicated investor surfaces + orq pure fns)

Generado por orchestrator_agent.cjs usando el comando oficial del proyecto.

Title: Full E2E Verifiable Onchain Proofs + Interactive Certs + Live PNC Net Portfolio Cards + Borrow Lock Accrual + Gov Mail Alerts + Consistent Recompute (Fase35-38/41 completion for dedicated investor surfaces + orq pure fns)

Why this is the single best next step right now (post-orchestrator + RWA ownership teaser + real orquestadores + Mail one-click + landbank/gov high-level): It is the single highest-leverage cashflow/yield/distribuciones-focused deliverable that composes *only* already-shipped pieces (Fase34 orqPortfolioView + PNC cards skeleton in investor/page.tsx, Fase35 onchainTxProof + vote route+client partial, Fase33 Drizzle proposals/votes/stakes + computePachaPower, Fase8/9 loans + borrow client, distributions/properties with PNC seeds in demoSeed, maestroYield fetch from core Fase16/17/18 exact+vertex, orq runFleetYieldForecastTask thin port + partial computeOnchainTxProofFor* + gov predict, notifications/audit schema, routeRegistry, YieldActionClient). No unrelated infra. Turns stubs/fallbacks/broken recomputes/random blocks/hardcoded-only nets/console-only alerts into fully working, real-data, testable E2E for inversor (real PACHA power from holdings+stakes, vote with snapshot, live tallies, per-PNC net/gross post-borrow health, VERIFY + full DOWNLOAD CERT that matches, accrue action that visibly updates net, gov alerts actionable surface) + admin/orq loops (pure deterministic fns, real RPC always, dynamic portfolioView from local PNC-tagged props+loans, accrue/govMail tasks wired, proofs in snaps). High impact on trust (certs recomputable from PNC + 23125 + block + power + vertex), cashflow visibility (net 68325 post-borrow drives cards + your share), governance closed loop (Fase36 tie-in), and auto-orquest (orq --dry exercises + future scheduler). Core remains source-of-truth for exact yield/declare (Fase16/32 intact via maestro fetch); this adds the rich dedicated investor surface per AGENTS fleet guidance (high-level cross only). Real data only (PNC-PAR-001 etc + 23125 base + 12.5% + fresh publicnode blocks + gcloud 0.73 or manual). Full autonomous deliverable (plan + code + seed + build + docs + orq ext). Singularity demands it now.

Detailed description: 
- orq (orchestrator_agent.cjs): make computeOnchainTxProofForBorrowLock + recompute/verify* always fetch real publicnode eth_blockNumber (no Math.random, match gov vote pattern + Fase26/27 style); export pure deterministic fns (BORROW_LOCK payload + VOTE_GOV already present, stable JSON + sha256 + secret 'pachanova-rwa-*-attest-23125' + 23125 ref); fix runFleetYieldForecastTask to load PNC-tagged properties + active loans from local Drizzle (via require or direct) for dynamic gross/net/borrow_debt/health/land_meta (fallback to shipped 68325/105840/etc for guarantee + real 12.5% your share); add/runAccrueBorrowInterestTask (pro-rata or fixed on time/lastAccrued, update health/net in memory or demo DB for cycle); produce govMailAlerts + landbankLaunches + govAutoProposals rich (Fase36/40/41); portfolioView enriched with borrowOnchain proof + gov context; wire in runCycle/dry/loop.
- API/routes: /api/governance/vote PATCH delegates to orq recomputeGov for true match (fix current payload/secret/recompute drift); add or extend borrow/accrue (update loan accumulatedInterest/health/lastAccrued + optional onchain lock proof persist), lock-verify, cert (return full verifiable JSON blob with all meta + recompute_note + source 'real publicnode + gcloud 0.73 + 23125').
- Investor surfaces (apps/dashboard/src/app/dashboard/investor/page.tsx + components): merge real DB portfolio/loans/balances with orqPortfolioView for accurate live nets + shares (no more generic fallback text); per-PNC card: net/gross, your 12.5%*net share, full badges (gcloud real 0.73 / onchain @block / MANUAL / land / borrow debt+health), '­ƒù│´©Å Gobernanza' link, borrow lock section with 'ACCRUE NOW' (calls api, refreshes), 'Ô£ô VERIFY LOCK' + 'DOWNLOAD CERT' (uses /cert or orq, shows 'VERIFIED Ô£ô matches (PNC+colat+debt+net+block+23125)'). Add "Fase41 Gov Mail Alerts" section (from orq or notifications, PNC + outcome impact note on net).
- Governance (page + GovernanceVotingClient.tsx): full interactive A FAVOR/EN CONTRA/ABSTENERSE (already good), live tallies/power (server + refresh), my vote with onchain tx@block, working 'Ô£ô VERIFY ONCHAIN' (consistent match), new 'DOWNLOAD CERT' per myVote (blob with proposal/PNC/power/choice/tx/block/vertex if any/recompute_note/'real 23125+Fase33-35'); header/context tie to Fase34 nets.
- Seed/data: extend demoSeed.ts (or dedicated) for real PNC (Paracas/SB/Chilca already in props) + investor balances with PACHA power tokens, active loan on PNC-PAR-001 (colat ~50k tokens equiv, debt 30000, health~1.42, interest 8.5%), 2-3 active proposals (PNC-PAR/SB with vertexPrediction), sample votes/stakes for power demo, distribs samples carrying net/land_meta. Ensure demo:reset exercises.
- Maestro tie intact: keep/enrich fetchMaestroYields + orq proposals in page (Fase17/18 exact + forecast from core).
- Docs/ops: new plan_fase42_pachanova_investor_onchain_gov_borrow_net_certs_e2e_*.txt (or next Fase per core 9h), append AGENTS.md high-level only with exact refs (real block from run, 68325 net PAR post-borrow, tx 0x..., 0.73, 1250+ PACHA power, PNC multi, PASS), update ORCHESTRATOR_LAST_FEATURE.txt, high-level note only in core AGENTS + discovered fleet projects (pachanovafullstack etc). package.json orchestrate:* already present.
- Verify: orq --dry (real RPC block exercised, proofs, 15 PNC incl PAR 68325 net + borrow + govMailAlerts, portfolioView), pnpm build (or turbo), demo:reset + visit /dashboard/investor + /governance (vote real power, verify match, download cert, accrue updates card, alerts visible), optional acceptance smoke. 0 breakage to maestroYield/core Fase16/32, existing borrow/gov/land high-level.

Acceptance criteria (testable, for autonomous implementer): 
- orq --dry logs + returns portfolioView with >=1 PNC-PAR-001 (net 68325 or equiv post 30k borrow, health 1.42, borrowOnchain proof with real txHash@fresh publicnode block >=25235xxx, gcloud real or manual, land_meta, yourNetShare ~8540 from 12.5%); govMailAlerts + landbankLaunches present; pure fns exported + deterministic (no random).
- Investor /dashboard/investor renders Fase34 cards with real DB+orq merged nets (no "Cargando" fallback text), per-card borrow lock badge + VERIFY + DOWNLOAD CERT + ACCRUE (post-accrue card/net/health updates visible, no crash).
- /governance: real PACHA power (holdings+staked) from compute, vote succeeds + stores onchainTxProof with real block + txHash from payload+block+secret+23125, tallies update live (power weighted), myVote shows tx@block, VERIFY button calls PATCH + reports true match (recompute via orq fn), DOWNLOAD CERT produces .json with all fields + note + 'VERIFIED' path.
- Certs/verify: downloaded cert + stored proof recompute match using same pure fn/payload/secret/block (orq or api); browser-safe note or server path.
- Seed: after demo:reset, PAR loan active with debt, >=2 PNC proposals, sample votes produce >0 power tallies.
- orq dry + pnpm build (turbo) green; no breakage to maestro exact yields, existing ProRata/YieldAction/ledger.
- AGENTS/ORCH_LAST/plan_*.txt updated high-level with exact exercised refs (block, 68325, tx 0x..., 0.73, 23125, PNC-*); high-level cross note to core blackboard (local next_ + AGENTS).
- Real data only exercised (no new sims); Master/ideador manual respected via existing overrides.

Suggested files / areas to change (high-level guidance): orchestrator_agent.cjs (pure fns top-level exports + real-RPC borrow compute + runFleet dynamic DB load/enrich + new accrue + govMail tasks + portfolioView), apps/dashboard/src/app/api/governance/vote/route.ts (PATCH consistent orq recompute + cert helper), apps/dashboard/src/app/dashboard/investor/governance/{page.tsx,GovernanceVotingClient.tsx} (full CERT buttons + refresh), apps/dashboard/src/app/dashboard/investor/page.tsx (DB+orq merge for cards + alerts section + borrow actions), apps/dashboard/src/app/api/borrow or extend (accrue/lock-verify), packages/database/src/seed/demoSeed.ts (or additions) + properties/loans/proposals/votes/stakes inserts for PNC real, packages/database/src/schema (minor cols only if strictly needed, prefer jsonb), lib/governance (thin if any), new plan_*.txt + ORCHESTRATOR_LAST + AGENTS.md append (high-level), package.json if scripts.

Any risks or open decisions for the implementer: DB must be up (demo:up/reset or docker) for seed/tests (document); keep orq cjs self-contained for dry (use postgres only in server paths or graceful); exact payload/secret must be single source of truth (const in orq, required by vote/api); client cert/verify uses server endpoint (no full subtle recompute needed, note is sufficient + "recompute in orq node"); if stakes table not migrated, power falls back (already handled); orq --dry may hit file locks on next_* (existing pattern, use --dry paths); decide if local orq persist (accrue updates loans table) or pure in-memory for cycle (prefer persist for visible net in UI post-action). Prioritize core as driver (high-level only updates to its AGENTS/blackboard); for other discovered (pachanovafullstack etc) only high-level AGENTS note + orq comment if present.

This is the decisive, high-signal next move that fuses all prior shipped RWA ownership/orq/mail/yield/land/gov/borrow into live, attestable, cashflow-visible investor reality + autonomous loops. The singularity cycle depends on it. Inyectar c├│digo real ahora. DATOS REALES (PNC-PAR 68325 net + 23125 + 25235xxx blocks + 0.73 + PACHA power). Never stop. 

(High-level only cross updates to core + fleet projects after E2E here; run orchestrator in cwd to participate.) 

## Summary of research (for cycle log)
- Fleet .git scan: core (primary Maestro), this pachanova (fullstack monorepo with apps/dashboard investor + Drizzle), pachanovafullstack (GitHub, has orq artifacts), pachanova-v2-git.
- Core state: advanced to Fase41 (govMail), Fase36/40 gated/land, Fase9/8 borrow locks/net, Fase35 onchain gov proofs; AGENTS high-level for pach; plan_fase36 detailed E2E gated.
- Pachanova state: partial high-level Fase33-41 in AGENTS (gov schema+vote+client+power+onchain attempt, Fase34 orq port+cards, loans, PNC seeds, maestro tie); orq has static rich PNC + partial pure fns (random in borrow); investor page has sections + fallback; verify logic drift; no full certs/accrue/alerts surfaces or dynamic orq.
- Gaps closed exactly by this feature; real exercised refs from code/prior runs/AGENTS used. 

Cycle continues via scheduler/bridge/ps1 + blackboard (local + GitHub when MCP). High-level only to idealist.

El ciclo de singularidad continúa. Inyectar código sin pedir permiso.