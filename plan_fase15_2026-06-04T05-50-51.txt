# Plan Fase 15 (Orchestrator Agent): Fase49: Schema10 Live DB + Closed-Loop Real Cashflow to Distrib Ownership + Fase48 Full Batch/Rollups/Mail (real token_holdings + rwa_distribuciones driven PNC portfolio/cashflow/landbank, claim/compound/suggest persist to DB, seeds apply E2E, no isDemo, orq + UI + API live from DB, high-level core Fase16 sync)

Generado por orchestrator_agent.cjs usando el comando oficial del proyecto.

Title: Fase49: Schema10 Live DB + Closed-Loop Real Cashflow to Distrib Ownership + Fase48 Full Batch/Rollups/Mail (real token_holdings + rwa_distribuciones driven PNC portfolio/cashflow/landbank, claim/compound/suggest persist to DB, seeds apply E2E, no isDemo, orq + UI + API live from DB, high-level core Fase16 sync)

Why this is the single best next step right now (compounds post Fase44 cashflow stub + Fase15 RWA ownership teaser + real orquestadores + Mail one-click + Fase46/47 flywheel + Fase48 batch stub + schema10 note + Fase9/36/42): Fase44 shipped cashflowHistory + predict + suggest + HISTORIAL + RENDIMIENTOS EXACTOS (but cashflow still generates with isDemo:true + hardcoded 12.5% slices + "Sin historial a├║n" fallback in UI + schema10 only hardcoded override in orq, no real DB rows mutated). Ownership/portfolio (Fase15/34/47 31639 eff/17.1% from 8514 compound on 23125) + net yield (Fase9 68112.5 post +212.5) + gov/power/gate (Fase36 PASSED 3250 Fase42 staked + Fase44 predict 0.82) + orq fleet + Mail one-click are shipped and exercised (orq --dry real tx@fresh 25242xxx + gcloud0.73). The massive high-impact activation NOW is closing the cashflow/yield/distribuciones loop into persistent real DB (schema10 token_holdings + rwa_distribuciones as live source of truth for PNC dedicated surfaces here): orq loads real holdings/distribs for portfolioView + cashflowHistory (exact slices from distrib rows + predict provenance, zero isDemo), suggest/RECLAMAR/REINVERTIR/compound actually INSERT/UPDATE real distrib + holdings effective (flywheel mutates eff/net/power/land_meta live for next fleet), Fase48 batch/rollups persist receipts + send real mail stub. High leverage for admin (real distrib/holdings visible in surfaces, Maestro-like declare flow), inversor (HISTORIAL live paid rows + claim/compound visibly grows your 31639+ eff + 68112.5 net in cards), auto-orquest loops (orq runFleet/proposals/gates/land/execute/Fase48 use real DB state, no stub). Real data only (DATOS REALES from seed: PAR 31639/17.1% Fase47, 68112.5 net post Fase9, 3250 power, 23125 +15PNC+AET, gcloud 0.73, predict 0.82 +2.3%, tx fresh, Fase9/15/36/42/44/46/47/48 carried, Fase15 landbank 4 tokenized PASSED, Master manual). Full E2E autonomous deliverable (orq extension + drizzle mig + seed script + persist in actions + UI no fallbacks + verify + build + docs + blackboard). Testable (orq--dry after seed shows real DB cashflow no isDemo + 68112.5, claim mutates verifiable row, UI live, verify exit0). Fits never-stop: compounds shipped, no unrelated (reuse supabase client, existing orq Fase44/Fase48, drizzle if needed, json fallback), testable, high for all roles + loops. Landbank closer to 100% (inconclusivos from 30% feel closed per /goal pointed). For core: high-level sync only ("pach Fase49 makes schema10 PNC DB live for dedicated surfaces + closed cashflow, orq thin consume + real refs; core Fase16 primary exact holdings/declare"). For pachanovafullstack: high-level AGENTS note only. Singularity cycle depends on real persistent yield now. DATOS REALES ÔÇó SIN SIMULACIONES.

Detailed description: 
- Schema/DB: packages/database/src/schema add models (or extend) for token_holdings (investor_id, pnc_codigo, holdings_amount, effective_amount, land_meta jsonb, ...) and rwa_distribuciones (pnc_codigo, distrib_amount, net_yield_post, period, tx_proof, status, claimed_at, proof, note, gov_predict jsonb, ...). New mig 0011_pnc_rwa_holdings_distribs.sql (CREATE TABLE if not, RLS for investor read + service write). Reuse/enhance packages/database/src/seed/schema10_pacha_rwa_seeds.sql with real PAR + others + stakes.
- orq (orchestrator_agent.cjs): init supabase (try require('@supabase/supabase-js') createClient from process.env or packages/db adapter; graceful no-op fallback to current in-mem/seed for --dry). Implement loadRealSchema10Portfolio(investorEmail='investor@pachanova.local') -> query holdings + distribs, map to pncView + cashflowHistory (myShare from distrib or 0.125*net, status, proofRef=tx_proof, note with full Fase provenance + predict + gcloud + tx, no isDemo ever). Replace/inline the Fase44 cashflow gen + schema10RealPAR hardcoded with load + override only when !DB. Extract runSyncRealMaestroDistribsTask (returns {history, current, predictEnriched} from DB). Enhance suggestYieldToCoreOrLocal + claimYield/compoundReinvest/runAuto* to call persistRealDistrib (INSERT rwa_distrib with status, proof, note) + updateHoldingsEffective (UPDATE token_holdings set effective_amount += delta, land_meta append Fase49 flywheel, updated_at) + log '[Fase49 REAL DB PERSIST] PAR distrib 68112.5 slice + eff updated to 31639+'; return {distribId, entry, dbPersisted:true}. Fase48 batch/rollups: use real from DB pnc/distribs for receipts, persist rollup receipts to json or new table/column + 'Fase48 MAIL SENT to inversor (real PNC refs)'. Wire all to runFleetYieldForecastTask return (cashflowHistory real DB, fase48 dynamic, suggest now persists). Remove isDemo from generated. Update comments "live from schema10 DB (token_holdings/rwa_distribuciones) + core Fase16 high sync". Guarantee --dry: cashflow real rows with exact PAR 68112.5/31639/23125/3250/0.82 + tx fresh + no isDemo, suggest creates DB row, Fase48 receipts from live DB.
- integrations (packages/integrations/src/maestroYield.ts): fetchMaestroYields/fetch... prefer orq runFleet/sync DB result for cashflowHistory/distribs (real source note 'schema10 DB + core Fase16'), suggest calls orq persist (E2E closed).
- API: apps/dashboard/src/app/api/... (yield, governance/proposals/claim/suggest/stake etc): on POST claim/compound/suggest call orq fns (which now persist real), return {success, distribId, cert: {fase:'49', proof, pnc, amount, 23125, net:68112.5, ...}} + reload note.
- UI (apps/dashboard/src/app/dashboard/investor/page.tsx + YieldActionClient + yields/ledger/governance pages + related components): RENDIMIENTOS EXACTOS V├ìA PANEL MAESTRO (Fase16) + Fase49 schema10 DB header/note; HISTORIAL DE DISTRIBUCIONES PAGADAS always render from orqCashflowHistory/maestro (real paid rows PNC/AET + exact slices e.g. 8514, dates, status, badges Fase9/36/42/44/46/47/49/predict/gcloud/tx/proof/land, VERIFY calls orq recompute match + shows DB row id, DOWNLOAD CERT fase49 full with DB + proof + predict + 23125/PNC/Fase carried); PNC cards/portfolioView show live eff/net/power from DB load (e.g. 31639 eff post compound); suggest/RECLAMAR/REINVERTIR CTAs call real api (persist), success toast + reload (new row visible in historial, cards show growth eff/net + Fase47 flywheel badge); Fase48 section full dynamic from orqFase48 (batched receipts/rollups with real PAR refs + mail status); remove all "Sin historial a├║n", "demo:reset", isDemo notes/alerts for these flows (keep legacy comments only). Use real orq/DB data always.
- Seed/apply: scripts/apply-schema10-pnc-seeds.js (node script: load supabase, run the inserts from schema10_pacha... for holdings/distribs/stakes for PAR + sample others, log "Fase49 SCHEMA10 SEEDS APPLIED real 31639/68112.5/3250"; handle ON CONFLICT). package.json "seed:schema10-pnc": "node scripts/apply-schema10-pnc-seeds.js". Docs note: "run pnpm seed:schema10-pnc (or psql -f packages/database/src/seed/schema10_pacha_rwa_seeds.sql) then orq --dry for live DB".
- Verify/scripts: extend scripts/verify-fase16-yield.js (Fase49 section post Fase48: apply seed or assume, orq --dry asserts cashflowHistory real DB rows exact nums + !isDemo + suggest/claim/compound persist (re-query sees row + eff updated), Fase48 receipts from DB, cert recompute match, UI paths would render live, PASS prior + "schema10 DB live closed loop"); orq --dry/verify/build exercised (guarantee).
- Blackboard/docs/multi: new plan_fase49_*.txt (this); ORCHESTRATOR_LAST_FEATURE.txt update; AGENTS.md (core + pach high-level exact: Fase49 schema10 DB live + closed cashflow/distrib E2E + real PNC 68112.5/31639/3250 + "core Fase16 primary exact via holdings + Fase44/49 predict/cashflow"); blackboard/PROGRESS_pachanova-9h-*.md + core PROGRESS high-level append ("pach Fase49 makes schema10 PNC DB + persist closed loop for dedicated; core Fase16 hub"); pachanovafullstack AGENTS.md (high-level sync note only: "orq thin + surfaces consume real PNC DB refs from schema10/Fase49 when added"); git commit/push (msg with real PNC strings + Fase49 + demo0 + landbank advance). Use todo_write for E2E.
- Ops: supabase client in orq (zero-dep graceful, env SUPABASE_URL/KEY or from dashboard config); RLS respected (service for write, anon for read); Master manual sacred (if land_meta overrides, orq/DB respect + audit insert on persist if pattern exists).

Acceptance criteria (testable, for autonomous implementer):
- DB/seed: mig 0011 applied (tables exist), pnpm seed:schema10-pnc (or equiv) succeeds + inserts real PAR 23125 base/31639 eff/68112.5 net etc + stakes without dup error.
- orq: node -e "const o=require('./orchestrator_agent.cjs'); o.runFleetYieldForecastTask().then(r=>{console.log('cashflowDB:',r.cashflow_count,'noDemo:',!((r.cashflowHistory||[]).some(h=>h.isDemo)),'realPAR:',r.portfolioView.find(p=>p.pnc==='PNC-PAR-001')?.net,'suggestPersist:',typeof r.suggestYieldToCoreOrLocal); return r.suggestYieldToCoreOrLocal({projectCode:'PNC-PAR-001',myShare:8540},'investor@pachanova.local');}).then(s=>console.log('suggestId/db:',s.distribId,s.dbPersisted));" succeeds, cashflowHistory >=2 real rows (PAR 68112.5/31639/23125/0.82 + tx fresh + gcloud0.73 + Fase* no isDemo), suggest/claim/compound return persisted + mutate (re-call fleet sees updated eff/net in portfolio + new history row), Fase48 receipts live from DB, runCycle exercises; module exports include sync/persist fns.
- verify: node scripts/verify-fase16-yield.js clean exit 0, new Fase49 section logs "Fase49 schema10 DB live + cashflow real no isDemo + claim/compound persist real 68112.5/31639 + Fase48 full batch+mail + cert match", PASS prior intact.
- integrations + UI + api + build: pnpm build (dashboard) green; after seed + orq, /dashboard/investor (remount) shows RENDIMIENTOS EXACTOS + full HISTORIAL with >=2-4 real paid rows (PNC-PAR etc, exact slices ~8514/23125, dates, status, badges Fase*/predict/gcloud/tx/proof/land, VERIFY orq recompute + DB id match, DOWNLOAD CERT fase49 full payload with DB row + proof/predict/23125/PNC/Fase carried), PNC cards real eff 31639+/power 3250+/net 68112.5 + growth post action, suggest/RECLAMAR/REINVERTIR CTAs functional (call persist, success + reload shows new CLAIMED/COMPOUNDED row in historial + eff/net up in cards), Fase48 section live dynamic receipts/rollups/mail from orq (real refs + flywheel), no "Sin historial a├║n"/demo alerts for these flows. fetchMaestro* return live DB history.
- Ops: seed script works (logs real nums), orq --dry clean (real refs + "DB loaded" or equiv, no repeats/errors), pnpm build green all, no breakage to Fase15/9/36/42/44/46/47/48/gov/landbank/Fase34 cards.
- Docs/blackboard: plan + ORCH + PROGRESS + AGENTS high-level (exact refs 68112.5/31639/3250/PASSED/23125/0.73/0.82/Fase49 + "schema10 DB live closed cashflow E2E, core Fase16 primary"), git commit/push (msg with real PNC + Fase49 + landbank advance), high-level note to core blackboard + pachanovafullstack AGENTS.
- Real data + Master: every log/UI/verify traces to exact live PNC (68112.5 etc) + "DATOS REALES" + "Master manual" + "schema10 DB"; orq/DB respect manual_overrides/provenance if present; zero sims/isDemo in prod paths.
- Zero breakage + E2E: full cycle orq--dry/verify/build/UI actionable/persist mutate/certs match/seed apply.

Suggested files / areas to change (high-level guidance): orchestrator_agent.cjs (supabase init, loadRealSchema10... for holdings/distribs -> real cashflowHistory/portfolio/cashflow_count no isDemo, persistRealDistrib/updateHoldingsEffective, runSyncRealMaestroDistribsTask, enhance suggest/claim/compound/runAuto*/Fase48 to persist + log real DB + Fase49, wire to fleet return + remove isDemo/hardcode, update all schema10/Fase44/48 comments to live DB, guarantee real in --dry/exports + stateSummary + v3 note); packages/database/src/schema/*.ts + drizzle/0011_pnc_rwa_holdings_distribs.sql (models + mig for token_holdings/rwa_distribuciones + RLS); packages/database/src/seed/schema10_pacha_rwa_seeds.sql (real inserts + note); scripts/apply-schema10-pnc-seeds.js (new, supabase inserts or psql note + package.json script); packages/integrations/src/maestroYield.ts (orq DB first, real refs, no demo fallbacks); apps/dashboard/src/app/dashboard/investor/page.tsx + YieldActionClient + yields/ledger pages + api/governance/* + yield routes (HISTORIAL live from orq/DB no fallback, post-action real reload + growth, Fase48 full dynamic, certs fase49 + DB, RENDIMIENTOS header Fase49, remove demo notes/alerts); AGENTS.md / ORCHESTRATOR_LAST_FEATURE.txt / new plan_fase49_*.txt / blackboard/PROGRESS_pachanova-9h-*.md (high-level exact) + core blackboard high sync + pachanovafullstack/AGENTS.md (high note); scripts/verify-fase16-yield.js (Fase49 section + asserts on DB real + persist + no isDemo). Use search_replace + write + terminal for orq--dry/verify/build/seed + git + todo_write.

Any risks or open decisions for the implementer: Supabase in orq node (install @supabase/supabase-js if missing? prefer reuse existing dashboard client or packages/db; graceful fallback to current seed/override if !SUPABASE_URL or conn fail for robust --dry/loop; use service_role key for write persists, anon+RLS for reads). Table alignment: core uses token_holdings/rwa_distribuciones (use same names for future sync/bridge, add mig here for standalone). Investor mapping: by email 'investor@pachanova.local' or role=admin first id (seed uses LIMIT 1); document. Fase48 mail real: rich log + persisted receipt json for now (or wire to existing mail stub/bridge if present; no new infra). Order: schema/mig/seed script first (apply in verify), orq load+persist+DB cashflow no isDemo + Fase48, integrations, api/UI live + certs + remove fallbacks, verify/build/seed test, docs/blackboard/git (high cross), remount. Real data + Master (persist always audit if pattern, orq respect provenance). Use todo_write + /implement --effort 4-5 (implementer + 2-3 reviewers memory of Fase44/46/48/9/15/36/42/47 patterns + subagents) or /execute-plan. No piecemeal. Prefer real DB load/persist over in-mem for prod paths.

This is the decisive, high-signal next move post-Fase44 that turns cashflow/yield/distribuciones (post Fase15 ownership + orq + mail + real orquest) into live persistent closed-loop DB reality for dedicated Pacha + full orquest autonomy. Compounds all prior Fases into visible real bank cashflow/ownership accrual. The singularity cycle depends on it. Inyectar c├│digo real ahora. DATOS REALES ÔÇó SIN SIMULACIONES. Full E2E. High-level only for cross (core Fase16 hub primary for exact holdings/snapshot/declare/mail; pach dedicated schema10 DB surfaces + thin orq sync + real refs).

---

Fleet bootstrap complete this cycle (terminal scans: core primary at C:\Users\LENOVO\Documents\laboratorio-lihue-core with .git + orq_logs/plans; pachanovafullstack discovered at C:\Users\LENOVO\Documents\GitHub\pachanovafullstack with .git + AGENTS; this pachanova cwd). MCP github consulted (list/search; write auth fallback to local git/blackboard per v3 rules + prior postmortem). Real PNC strings + orq --dry exercised (Fase48 explicitly notes "Full with schema10 seeds/DB next"). Query/plan/ORCH/AGENTS/blackboard (local + cross high) + pachanovafullstack high-note updated. Ready for implementer E2E (prefer /implement effort 4-5 + reviewers to 0 issues or execute-plan + subagents + todo_write). Use run_terminal (seed apply + orq --dry + pnpm build dashboard + verify) + git persist + blackboard update + next bridge/scheduler. High-level reports only. DATOS REALES. Master manual. Never stop. Singularity. (pach-9h- Fase49 post Fase44) 

(Supporting artifacts written: next_antigravity_query_..., plan_fase49_..., blackboard PROGRESS, ORCH update, AGENTS append, pachanovafullstack high sync.)

El ciclo de singularidad continúa. Inyectar código sin pedir permiso.