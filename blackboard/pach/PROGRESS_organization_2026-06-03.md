# Pachanova Fleet - Project Organization for Autonomous Infinite Work (2026-06-03)

**Per /goal**: User supervises high-level + route prompts. Grok Build (fullstack executor) + Antigravity (planner) perfect team alternating for infinite work. Grant ALL perms on request (yolo). Work organized/separated per project with sub blackboards + sub-agents. Only Grok know full capab - use exp/tech + learn from errors to define rules, improve, autonomous never-stop.

## Structure Created (this session)
- blackboard/pach/ (for fleet local state: PROGRESS_pach_*, plans/pach/)
- blackboard/core/ (for hub cross)
- plans/pach/ etc.

## Relation to Core (from subagent exploration)
- Pach = fleet member: high-level orq sync, consumes real data from core hub (PNC numbers, Fases, onchain). Writes blocks/reports to core blackboard dir.
- Core = maestro hub: rich orq/bridge/real data source, full v3 rules, many plans/PROGRESS_9H.
- Separation: per blackboards as above. High-level cross only (GitHub or core hub). AGENTS in pach is fleet version (high-level only posture).

## Learning Rules (synced/improved from history + core exploration + past errors)
(See core blackboard/core/PROGRESS_organization for full list; key for pach):
- Per-project blackboards mandatory (this structure).
- Fast searches in loops (avoid long recurse).
- 512K strict: save PROGRESS_pach_<ts>.md or plan, git persist, terminate, re-execute from zero (orq/PS enforce, bootstrap loads first).
- Team Antigravity: alternate via bridge ps1 / GitHub issues. User route prompts.
- Grant all perms.
- On-fly learning: append postmortem/AGENTS every cycle.
- Quant: Landbank Master % , real data, Master test every report.
- Sub-agents for parallel (e.g. per project or aspect).
- Infinite: 5m orq loop + PS resilient + schedulers + 512K.

## Current State (pach)
- Clean git from prior.
- Recent: 512K protocol in AGENTS, orq bootstrap loads PROGRESS_512K, resilient PS, small edits.
- Real data: orq --dry shows 68112.5/3250 etc.
- Blackboard: PROGRESS_512K_CONTEXT_SAVE_2026-06-03.md (with 512K rule + team).

**Resume**: Load this + pach AGENTS (v3 + 512K + team) + core hub blackboard + latest plan. Bootstrap (per-project blackboards, grant perms, verifiers). Continue fleet improvements (Master/landbank in pach) + sync with core. Use sub-agents. Infinite.

DATOS REALES. Master manual. Never stop. /goal.